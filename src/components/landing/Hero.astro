---
import Button from '../ui/Button.astro';
---

<section class="relative pt-32 pb-40 md:pt-48 md:pb-64 overflow-visible flex flex-col items-center justify-center min-h-[80vh]">
  <div class="container mx-auto px-6 text-center relative z-10 max-w-5xl">
    <div class="animate-slide-up">
        <h1 class="text-5xl md:text-8xl font-bold tracking-tight mb-8 text-[var(--color-text)] leading-[1.1]">
          Organize Your <br />
          <span class="bg-clip-text text-transparent bg-gradient-to-r from-[var(--color-text)] via-[var(--color-secondary)] to-[var(--color-text)] bg-300% animate-gradient">
            Digital Universe.
          </span>
        </h1>
        
        <p class="text-xl md:text-3xl text-[var(--color-secondary)] mb-12 max-w-3xl mx-auto font-medium leading-relaxed tracking-wide anim-delay-100 animate-slide-up opacity-0" style="animation-fill-mode: forwards;">
          The universal media manager. <br class="hidden md:inline" />
          Transform chaos into a pristine library.
        </p>
        
        <div class="flex flex-col sm:flex-row items-center justify-center gap-6 anim-delay-200 animate-slide-up opacity-0" style="animation-fill-mode: forwards;">
          <Button href="/download" variant="primary" size="lg" class="min-w-[200px]">
            Download for Windows
          </Button>
          <Button href="#features" variant="secondary" size="lg" class="min-w-[200px]">
            Learn more
          </Button>
        </div>
        <p class="mt-6 text-sm text-[var(--color-tertiary)] anim-delay-300 animate-slide-up opacity-0 font-medium" style="animation-fill-mode: forwards;">
          macOS and Linux support coming soon.
        </p>
    </div>
  </div>
  
  <!-- Subtle Background Glow -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60vw] h-[60vw] bg-[var(--color-accent)]/20 rounded-full blur-[120px] -z-10 opacity-60"></div>
</section>

<style>
  .bg-300\% {
    background-size: 300%;
  }
  @keyframes gradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  .animate-gradient {
    animation: gradient 8s ease infinite;
  }
</style>
